name: Build and push Docker images

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/login-action@v3
        with:
          registry: gitverse.ru
          username: ${{ secrets.GITVERSE_USERNAME }}
          password: ${{ secrets.GITVERSE_TOKEN }}

      - uses: docker/setup-buildx-action@v3

      - name: Build and push kvault
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/qvarkk/kvault:latest
            gitverse.ru/qvarkk/kvault:latest
          cache-from: type=gha,scope=kvault
          cache-to: type=gha,mode=max,scope=kvault

